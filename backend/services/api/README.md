# API service (Phase 1 baseline)

Current endpoints:

1. `GET /api/health`
2. `GET /api/ready` (checks PostgreSQL connectivity)

Next phase:

1. auth login/refresh/logout
2. CRUD for tenant/site/vessel/user/package
3. usage and monitoring query endpoints
